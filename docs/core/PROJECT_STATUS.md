# Project Status

**Last Updated:** June 5, 2026
**Current Phase:** Beta — live in production
**Current Version:** **3.0.67** (deployed 2026-06-05, commit `c455813`, user-deployed). Fixed the Final Review "Accept does nothing" crash. v3.0.66 Phase 2.1 reintroduced the v3.0.61 crash by deleting v3.0.62's inline Stage-1 gate override on the false premise that `validateMove` would "agree by construction" — it doesn't for the `moveIntent` path (intent set at roll-time, validated at END-TURN against possibly-changed approval state, then rejected). `MovementExecutor.executeMovement` now reconciles a stale `moveIntent` against the live `getValidMoves` before `movePlayer` (valid intent moves; single forced destination / gate reroute wins; ambiguous stale intent cleared as no-move instead of throwing). Full-flow regression in [tests/ghost/finalReviewStaleIntent.test.ts](tests/ghost/finalReviewStaleIntent.test.ts). Diagnosed entirely from the bug-reporter **screenshot** — the `/api/public/feedback/open` summary strips screenshots/console/gameState; fetch the full `/api/feedback/<id>.json` record. Same release fixed the bug reporter silently dropping console logs (a `useCallback` stale-closure on the opt-in flag; checkbox now defaults on + the success toast confirms attachment). Strict ghost gate **passed**; the `try-again-happy` ghost gate is **pre-existing red** (32/50 wins, verified identical on v3.0.66 baseline + overruns its 15-min timeout) — logged in TODO, **not a regression**. Targeted sweep 1602/1602 (canonical) / 1663/1663 (broad), typecheck + build clean. **Prior (v3.0.66, commit `a5518f3`):** Two-version session executing the parallel-systems audit catalogued at the end of v3.0.64. **v3.0.65** shipped Phase 1: (1.1) audit found the "visitType drift trap" framing was wrong (`hasPlayerVisitedSpace(destinationSpace)` and `player.visitType` answer different questions — destination's prior-visit status vs current space's visit type — there's no parallel system to delete; pinned a small data integrity invariant via [tests/services/MovementService-visitTypeInvariant.test.ts](tests/services/MovementService-visitTypeInvariant.test.ts) without touching production code); (1.2) lifted log-display filter rules into a shared helper [src/utils/logFiltering.ts](src/utils/logFiltering.ts) (`getDisplayableLogEntries`) consumed by `PlayerLogSection`, `GameLog`, and `PostGameLogViewer`, canonical rule `isCommitted && visibility === 'player'` aligned with v3.0.63 log-honesty theme. Behavior change for the in-game GameLog admin toggle: no longer shows mid-turn provisional entries until they commit at end-of-turn (or vanish on Try Again). **v3.0.66** shipped Phase 2.1: collapsed `MovementService.getValidMoves` and `MovementService.getDiceDestination` into ONE resolver. `IMovementService.getValidMoves(playerId, options?)` now accepts an optional `{ diceRoll }` — on dice/dice_outcome spaces it narrows the base set to that single roll's destination BEFORE the override stack (lock-point filter, resume-hub augmentation, Stage-1 approval gate) runs. `MovementExecutor.executeMovement` dice branch consumes the unified resolver; the v3.0.62 inline gate block is deleted. Same commit included a 435-line dead-code cleanup: grep for callers of `getDiceDestination` surfaced a 3rd caller in `PlayerActionService.handlePlayerMovement` whose parent `rollDice` method had zero src callers — superseded by `DiceRollProcessor.rollDiceWithFeedback` (decouples roll from movement; movement now deferred to END TURN via `MovementExecutor`). Deleted `rollDice`, `handlePlayerMovement`, `endTurn` from `PlayerActionService.ts` + their interface entries + their tests (kept one test asserting `playCard` doesn't sneakily end the turn). Targeted sweep **1657/1657 green**, typecheck clean, ghost strict gate **PASSED** (50 random games, exit 0, ~13 min wall clock — well-pinned the v3.0.62 family of bugs). Live playtest verified through 4 spaces: OWNER-SCOPE-INITIATION → OWNER-FUND-INITIATION (fixed) → PM-DECISION-CHECK (choice) → ARCH-INITIATION, no errors. Parallel-systems audit now **4 of 7 closed** (movement merge, log filter, visitType, plus v3.0.64's canEndTurn de-duplication). Phase 2.2 (TurnTransaction boundary — state TEMP/REAL + log session unification) remains queued. **Prior summary retained below.** **3.0.64** (deployed earlier 2026-06-04, commit `658bc9a`). Three-version session driven by a real production crash report on v3.0.61, then expanded into a structural-debt audit. **v3.0.62** closed the residual FINAL-REVIEW crash (v3.0.61's fix patched `getValidMoves` but missed `MovementExecutor.executeMovement`'s dice path — same gate-bounce destination, different code path, downstream `validateMove` still threw on FINISH-from-FINAL-REVIEW with a missing approval) AND shipped the movement-choice "show last" UI gate from `fb:55b6626f` (new `gameState.movementChoiceUnlocked` flag computed in `StateService.calculateRequiredActions`; three UI surfaces — `ActionCenterPanel` destination picker, `BoardCanvas` green next-move arrows, TV display via shared state — suppress the choice UI until every non-movement required action is complete; pure-choice spaces unlock immediately, dice spaces unaffected). **v3.0.63** closed the v3.0.62 Test #3 architecture gap: new `LoggingService.discardCurrentSession()` symmetric with `StateService.discardTempState`, called from `TurnService.tryAgainOnSpace`, removes the abandoned attempt's pencil log entries so the post-game log no longer narrates rolled-back actions. Test #3 flipped from `it.fails` to passing; dev banner in PostGameLogViewer removed. **v3.0.64** dropped a third instance of the same architectural pattern — `ActionCenterPanel` was re-deriving `GameRulesService.canEndTurn` line-for-line — and logged 5 more parallel-systems candidates to TODO under "Parallel-systems audit" alongside the two already-tracked structural debts (movement-resolver merge, state/log unify). All seven debts share the same architectural theme and should probably be tackled together in one dedicated session. **TACTICAL pattern added to CLAUDE.md (v3.29):** parallel-systems audit recipe — diagnostic greps + symptom map for movement / state-log / canEndTurn / visitType subsystems. Targeted sweep 1653/1653 green across all three deploys (was 1645 + 1 expected fail at session start; the +1 came from removing the expected-fail). **Prior summary retained below.** **v3.0.61** (deployed 2026-06-02, commit `3c70db9`). **v3.0.61 is a production bug fix discovered by wiring `ApprovalService` into the ghost regression bot.** Workstream 7 Phase 7.4 Stage-1 gate at `REG-DOB-FINAL-REVIEW` correctly bounced a player without DOB/FDNY approval back to the missing examiner — but the routed destination wasn't in `MOVEMENT.csv`'s valid moves, so downstream `MovementService.validateMove` threw `Invalid move: REG-DOB-PLAN-EXAM is not a valid destination`. Real players who reached Final Review without DOB approval (legit via W-card scope-change revoke or L-card `revokes_approval`) would crash. Dead code in normal play but the ghost's random play exposed it: first 50-game run after the wire-up returned 29 wins / 15 hard failures vs deterministic baseline 39/0. **Fix is data-driven per "data-driven always" rule:** new `has_final_review_gate` boolean column on `GAME_CONFIG.csv` (lifted to SOURCE `Spaces.csv`), helper `DataService.hasFinalReviewGate`, `MovementService.getValidMoves` collapses valid moves to `[gate.routeTo]` when the gate fails (the gate discards the dice — player has no other choice). Real-world correct: missing approvals = forced back to legalize; no cheat escape at the final DOB check. Ghost strict gate post-fix: 50 games / 0 hard failures / wins ≥ 36. **Audit catches during the work (2 slip-throughs from prior fixes):** (a) `tests/server/processGameData.test.ts:259` asserted the old 6-column LOGIC_QUESTIONS header; the `auto_answer_from` column landed v3.0.33 but the sibling test was never updated — fixed. (b) v3.0.7 commit `b9d85cb` edited `{fundingAmount}` into `CLEAN_FILES/SPACE_CONTENT.csv` only, never carried to SOURCE; the regen step would have silently dropped it — token added to SOURCE; pipeline integrity restored. **TODO L117 partial:** 3 integration tests in `tests/integration/TransactionalLoggingFlow.test.ts` against real TurnService → LoggingService wiring. #1 (standard turn → commit) + #2 (Try Again → exploratory entries stay uncommitted) pass. #3 (multi Try Again → only final attempt committed) is `it.fails` documenting an architecture gap: `commitCurrentSession` sweeps all session entries regardless of attempt origin. Temporary "Dev note (remove before release)" banner in [PostGameLogViewer.tsx](src/components/game/PostGameLogViewer.tsx) flags the limitation; remove when `.fails` flips to passing. Targeted sweep 1645/1646 (1 expected fail), build + typecheck clean. Two new TACTICAL patterns added to CLAUDE.md: regen-as-audit, ghost-wire-up-exposes-dead-code. **Prior summary retained below.** **v3.0.56 → v3.0.60 was a 5-version polish + housekeeping session.** v3.0.56 fixed the `fb:30be69b2` arrow-overlaps-box complaint by wrapping `@jalez/react-flow-smart-edge`'s `SmartBezierEdge` in a project-local `SmartBezierEdgeTuned` that drives the underlying `SmartEdge` with `nodePadding=30` (upstream default 10 was ~7% of a 150px tile — too narrow for the A* router to detour). Verified live via DOM bbox check: PM-DECISION-CHECK → ARCH-INITIATION path sits 11px right of CHEAT-BYPASS's right edge, `edgesCuttingThroughCheat: []`. v3.0.57 fixed the TV-mode player chip readability bug surfaced by the TV-layout visual check that closed `fb:608bb670` — chip used 15%-opacity player-color background + name in `player.color` + stats in `colors.text.secondary`, all near-invisible on the blue header; swapped to glassy white backgrounds (`rgba(255,255,255,0.25)` current, `0.12` others) + white name + 85% white stats; border still uses `player.color` so the per-player ring signal survives. v3.0.58 hotfix corrected `server/server.js:455` referencing `currentVersion` from outside its declaring function's scope — same shape as commit 95f46f9's startup-log fix; `/health` had been returning 500 on every hit since v3.0.54 added the version field, silently breaking `scripts/check-sync.sh` and any external monitoring. v3.0.59 moved the player chip strip INTO `headerTopRow` next to the buttons (was a second-row band beneath them, felt orphaned) AND made the "Look at your phone" pill auto-dismiss 2s after each turn handoff via `showTurnPill` state + a `useEffect` keyed on `currentPlayerId` — the chip's 3px pulsing colored border carries the steady-state "whose turn" signal once the pill clears. v3.0.60 dropped the `overflowX: 'auto'` rule from `headerPlayerStrip` that was carried over from when the strip lived on its own full-width row — with the strip now sharing the top row alongside buttons + pill, the overflow rule made browsers reserve scrollbar space on both axes around a chip that clearly fit, leaving thin gray scrollbars with nothing to scroll. **Housekeeping wins also bundled:** 4 stale TODO entries reconciled to `[x]` against CHANGELOG (phone reconnect banner v3.0.54, opt-in console capture v3.0.54, `check-sync.sh` v3.0.54, backend version logging in `server/server.js`); 5 dashboard reports PATCH-flipped after verifying their shipped fixes (`fb:6e1e8ac4` chime user-verified single-player, `fb:9c961893` progress labels v3.0.53, `fb:58277eca` E-card silent fail v3.0.48, `fb:05a8b722` phone fullscreen v3.0.53, `fb:3a8f2b60` log merge v3.0.53 — all had been done but never flipped); `fb:30be69b2`, `fb:7c972948` (closed as v3.0.10 side-effect, stale duplicate at L316 removed), and `fb:608bb670` also flipped. Dashboard 15 → 7 unresolved across the session. The remaining 7 are all design-blocked or deferred (`fb:55b6626f` defer-movement-choice refactor, `fb:f22035af` expeditor-mechanic redesign, `fb:0aa9660c`/`fb:8ad42b52` onboarding Phase C authoring, `fb:46dd4a47` needs-fresh-repro, `fb:89d9f101` cheat-cluster grouping, `fb:d27a73d0` per-edge edit). Targeted vitest 1588/1588 (86 files); typecheck + build clean. Two TACTICAL patterns added to CLAUDE.md (scope-bug-in-route-handler, phantom-flex-scrollbar) plus a DOM-bbox-edge-routing verification recipe. **Prior summary retained below.** **v3.0.55** (deployed 2026-06-02, commit `2b99a30`). UI version label had been stuck on v3.0.51 across the v3.0.52–55 sprint because `package.json` was never bumped — the header reads `pkg.version` at vite build time, not from commit messages. Fixed via `2b99a30 chore: bump package version to 3.0.55` + redeploy. Same session also landed `2211b5f` adding `.gitattributes` + `core.autocrlf=input` to stop Windows CRLF↔LF churn that was producing ~30k-line phantom diffs across 44 source/test files (real-content diff = 0). See CLAUDE.md TACTICAL for both lessons. **v3.0.44 → v3.0.51 was an 8-version sprint in one session** — closed the 3-version `fb:91738221` plan (44 plain-English log strings → 45 expandable rows → 50 post-game viewer + export with MD/JSON/CSV/PDF formats), patched a missed dice-roll producer (46) and dedupe'd the double-Drew log entry (47) both surfaced by live playtest of v3.0.44, then triaged 3 new same-day dashboard reports (48 E-card affordability silent-fail with disabled Play button + tooltip + notification fallback; 49 TV setup scrollbar always-visible + one-time haptic-prime tap gate for phone-join browsers that block `navigator.vibrate` without a user gesture; 51 hotfix wrapping `sessionStorage` access in try/catch after Comet white-screened on the haptic gate's useState initializer). Two operational lessons hardened in CLAUDE.md TACTICAL: (a) always push before handing user the deploy command — Unraid's deploy script does `git pull` and v3.0.44+45 wasted ~30 min when commits stayed local; (b) browser-storage access inside hook initializers MUST be try/catch'd or you crash the whole tree on mount. Targeted sweep 1574/1574 (was 1553), build + typecheck clean. Vibration still doesn't fire on Comet even after the gesture-prime gate — tracked in TODO as a future investigation, audio cue fallback the likely path. **Prior summary retained below.** **v3.0.40 → v3.0.42 was a three-version run in one session — playtest-signal-driven Life Event modal receipts + recurring-effect callout (v3.0.40), then two rounds of 5-bug sweeps (v3.0.41 / v3.0.42).** v3.0.40 closed the biggest playtest gap from v3.0.39: Kids A–E richer life-event effects worked but were invisible. The LifeEventModal now renders a "What just happened" receipts block (money/time deltas, approval flips, card gains/losses, "this will keep affecting you" cue) — summary computed by snapshotting the player before/after `applyCardEffects` in CardEffectHandler and diffing into a `LifeEventEffectSummary[]`. Multi-turn duration cards (Kid C: L002/L004/L008) also now surface each re-fire as a notification + log entry ("🔁 Sick Kid still affecting you: -$2,000 — 2 more turns to go" / "— last one") via a new `surfaceRecurringEffect` helper in EffectEngineService. **v3.0.41** shipped 5 verified-real items: Kid C apply-time phase filter (L004 now correctly only ticks players in the CONSTRUCTION phase via a new optional `requiredPhase` on the RESOURCE_CHANGE payload, with duration preserved during out-of-phase turns); deep-clone defense for REAL/TEMP state arrays in TurnStateManager (new `cloneMutableState` helper closing 5 shallow-spread foot-guns surfaced by an external architecture audit); `cancelAllPendingChoices` hook on ChoiceService called from TurnService.endTurn (kills the 5-min reject-timer leak when edge cases leave choices unresolved); "Accept Owner Funding" label lifted to ModalConfig.csv + a case-sensitivity bug fix at FinancesSection.tsx (the strict `effect_action === 'draw_b'` check never matched CSV's `'draw_B'` — dead override exposed); stale START-QUICK-PLAY-GUIDE migration deleted (2 no-op methods + interface entries + test mocks). **v3.0.42** ran the same 5-item playbook: case-sensitivity bug fix in CardsSection (EXPEDITORS section badge was silently dead) + EventsSection (LIFE EVENTS section badge same shape); dead `apply*Effect` infrastructure purge (5 TurnService private wrappers + 6 SpaceEffectService methods + their 22 tests — net -579 lines, unreachable since v2.65.9's CONTRACTOR_UPDATE migration); 6 dashboard PATCH-flips reducing `/start` noise (fb:068a66f2, c7312a0a, f7312d82, adbc48b0, 1aad6035, 5dc01203); fb:46dd4a47 investigated (data + code paths look correct — PM-DECISION-CHECK IS a valid CHEAT-BYPASS dice outcome per DICE_OUTCOMES.csv rolls 5/6 punishment branch — downgraded to needs-fresh-repro). One honest mid-stream pivot: originally-planned `DOB_APPROVED_DESTINATIONS` / `AUDIT_TRIGGERED_FROM` lifts were dropped because TODO had already triaged them as defensible domain constants. Targeted sweep 1542/1542 green; build + typecheck clean. **External architecture audit also reviewed mid-session** — 1 real risk verified (the now-fixed shallow-spread), 1 minor risk verified (the now-fixed choice-cleanup leak), 1 false alarm rejected (claimed "Multi-path Click-to-Move Bug" doesn't exist — `executeMovement` is gated behind End Turn confirmation, not auto-fired by raw state updates). _Prior v3.0.39 summary retained below._ v3.0.39 was a "crush all bugs" multi-front block (18 items shipped): **v3.0.39 is a "crush all bugs" multi-front block (18 items shipped):** the top-3 from the v3.0.38 handoff (DiceRollProcessor literal swap to ApprovalService constant; Try Again now restores revoked approvals via 4 new `MutablePlayerState` approval fields + TEMP routing of all 3 write sites; **Life Event richer effects Kids A–E ALL DONE** — approval-revoke from auto life events, free E-card draws on L005/L007/L010, multi-turn duration L002/L004/L008 with a latent **N×N parser-vs-engine over-stack** bug fixed in the same pass, dice-conditional L009 via a new `diceRoll?` option on `applyCardEffects`, and forced-discard L003/L048 with a new `CardEffectHandler.autoPickForcedDiscards` flag that lets the headless ghost bot auto-pick while production keeps the per-player choice modal), 8 quick wins (🃏 emoji swap, dead puppeteer test deleted, two stale E2E entries resolved by passing on main, player-panel filter resolved by v3.0.15 design, `{fundingAmount}` adopted at BANK/INVESTOR Subsequent rows, 404-investigation cleared as not-a-bug, mojibake investigation cleared as game-server-clean), and the visit-indicator part 2 — `DiceResultModal` no longer renders the same numeric delta twice (once colored in Effects, once in BeforeAfterBlock). Strict ghost gate 50 games passed (zero hard failures, win-rate floor held). Targeted sweep 1536/1536 green. 4 dashboard reports PATCH-flipped. _Prior v3.0.35–38 block summary retained below._ **The v3.0.35–38 block** turned the v3.0.34 "investigated but not fixed" list into shipped, live-verified fixes plus a major correctness fix: **v3.0.35** — construction cost now recorded (was charged but clobbered on turn-commit), bank loans create `loans[]` records (so reg fees apply), end-game shows real Turns/Rounds/Score (verified live by cheating state to CON-INITIATION/FINISH); **v3.0.36** — dice-movement picker shows friendly names; **v3.0.37** — **Life Event (L) cards finally apply their money/time effects** (auto-draw never called `applyCardEffects`, so every L-card tick/money was a silent no-op — which is why the older L-card data fixes never changed gameplay; the fix applies only the money/time subset to avoid a choice-deadlock that hung the ghost gate, richer effects deferred), and the strict ghost win-floor was recalibrated 45→36 because the now-correct economy makes the random bot win less; **v3.0.38** — optional expeditor actions (Replace/Return) no longer gate the Move button (was a dead-end). _Prior v3.0.34 block below retained._ **v3.0.34** is a playtest bug-fix block from a full SETUP→FINISH playthrough. Headline: a gap in the `cardTextMatchesColumns` integrity gate (it only validated "each/all players" *global* cards) let **self-targeted "Draw N Expeditor Card"** cards ship broken — 5 L-cards had the count in `discard_cards` (and a bare number defaults to type W, so they discarded a Work Package) and 7 E-cards had bare `draw_cards=1` that drew **Work Packages instead of Expeditors** (inflating project scope). A new self-target DRAW gate flagged all 12; data fixed + flat-time L-cards (L038/L039/L031) corrected. Also: "Player Player 1" log doubling dropped; design-fee tooltip reconciled to the strict-any-phase 20% rule. **Investigated but NOT yet fixed (see TODO Playtest Bug Sweep):** construction cost IS charged (verified $907,500 exact against saved game G262) but never recorded in `expenditures.construction`/`costs`/`costHistory` — so it's invisible in the Pro Ledger and end-game "Total spent" (root cause: real-state `updatePlayer` write clobbered by turn-commit vs. design fees' `updateTempState`); bank loan creates no `loans[]` record so 1%-of-loan regulatory fees charge $0; end-game Turns-taken=1 / Final-score=0 / Total-spent-design-only; error-level skip logging; raw-space-code destination labels. Prior: **v3.0.33** fixes the game's one remaining hard blocker — `fb:068a66f2` "LOGIC_QUESTION yes/no buttons do nothing in TV+phone mode." Root cause, now code-confirmed (CLAUDE.md had it as unconfirmed): the question chain's pending promise is created during the OUTGOING player's `endTurn → nextPlayer → startTurn`, so it lives on that device — but the INCOMING player answers on their own phone, a different runtime with no matching promise. Fix is a state-mediated relay (`Choice.resolvedWith` + `StateService.setChoiceResolution` + a `ChoiceService` observer subscription) mirroring the existing `moveIntent` pattern; applies to all choice types, single-device path unchanged. Needs a two-device playtest past FDNY-FEE-REVIEW to confirm end-to-end before flipping the dashboard flag. Pile-B triage this session found the rest of the "open" functional reports were already fixed-pending-flag-flip (7 flipped via PATCH, dashboard 34→27) or design/data calls. **A second May 28 block (v3.0.28–32)** squashed five desk-fixable dashboard bugs: **v3.0.28** made L012 "Soil Contamination" +4 days conditional on the player holding a ground-disturbing W card (new data-driven `work_type_conditional` card_mechanic, `fb:776e3ba7`); **v3.0.29** stopped the board drawing a *superset* of next-move arrows — it now uses the same narrowed `getValidMoves` the panel does, via a tested `computeVisibleEdgeIds` helper (`fb:84da66be`); **v3.0.30** added a "↩ Visit #N" revisit badge to the panel header (`fb:0cdd59ba` pt 1); **v3.0.31** hoisted the TV player strip into the blue header (4→3 grid rows, board reclaims the band) and shrank the bottom space message (`fb:608bb670`, needs a TV visual check); **v3.0.32** added hover tooltips explaining the progress-bar colors via a tested `progressIndicators.ts` helper (`fb:f8491e74`). A recurring finding this block: several "open" dashboard items were already fixed in earlier versions but their TODO boxes were never flipped — 5 stale boxes corrected. Process note: the 5 commits were initially not pushed, so the first deploy attempt pulled only v3.0.27; pushed and the deploy-handoff memory was hardened with a `git status -sb` pre-handoff gate. The earlier May 28 block (v3.0.21–27) ran an instrument-then-fix arc. **v3.0.21** fixed the bug reporter to actually save the `consoleLogs` + `gameState` it was already capturing (the server POST handler had been silently dropping both fields) and surfaces a `consoleSummary` in the `/start` feed. That diagnostic capture immediately paid off: an absent `gameState` in report `fb:c7312a0a` ("won but phone showed nothing") proved the phone couldn't reach its own state, pointing at **v3.0.24** — the root-cause fix for a 3-report phone cluster. Mobile browsers freeze backgrounded tabs (heartbeat timer pauses, OS kills the socket without firing `onclose`), so a phone that locks/app-switches mid-game silently lost its WebSocket and never recovered; `WebSocketSyncService` now reconnects on `visibilitychange` (tab return-to-foreground), keeps retrying while visible instead of giving up, and the server's existing subscribe-pushes-state behavior catches the phone up for free. This MAY also resolve the open blocker `fb:068a66f2` (LOGIC_QUESTION buttons do nothing on phone) if its root was a dead socket — to confirm on reproduction. **v3.0.22/23** swept three voice-rule leaks from the v3.0.19 playtest: "Roll dice" button → "Determine Outcome" (`fb:adbc48b0`), Life Event modal's "(rolled N at SPACE)" parenthetical dropped (`fb:1aad6035`), TV player strip "6 cards" → "6 resources" (`fb:5dc01203b`). **v3.0.25** reworked the setup screen: `isSmartTV()` UA heuristic auto-preselects TV mode on real smart TVs (laptop-into-TV is undetectable → prominent enlarged toggle is the fallback), and TV mode now hard-blocks Start until every player's phone has joined (`deviceType==='mobile'`), with the QR caption flipped from "Optional" to "⚠ Required: scan to join". **v3.0.26/27** are the two halves of the board current-tile complaint (`fb:97fa9c75`): v3.0.26 inverted the color salience so the current tile is the hero (bold phase glow + tinted fill) and valid-move tiles are a quiet green outline (was: green fill out-shouting the bigger-but-pale current tile); v3.0.27 made the current tile fully open by default — grew it to the click-expanded footprint and added a `showsFullText` flag so its story + action description render untruncated (clicking now reveals nothing new). _History below retained._ v3.0.20 closes `fb:58a2112b`: LOGIC_QUESTION chain at REG-FDNY-FEE-REVIEW no longer asks the player questions the engine already knows (Q1 "Did you pass FDNY approval before?" + Q5 "Do you have DOB approval?" now auto-resolve from `player.fdnyApprovalStatus` / `player.dobApprovalStatus`). New `auto_answer_from` column on LOGIC_QUESTIONS.csv; engine consults via `MovementService.tryAutoAnswer`; auto-resolutions log to the game log so the player sees what the engine decided. Q2/Q3/Q4 still asked (no state tracker yet). v3.0.19 ships diagnostic toasts for `ChoiceService.resolveChoice` failures so `fb:068a66f2` ("LOGIC_QUESTION yes/no buttons do nothing") is debuggable on next reproduction — failure case (no active choice / id mismatch / invalid selection / no pending promise) surfaces visibly to the player instead of silent console.error. v3.0.18 closes `fb:ffdddd29` (TV setup: 4 players added, lower 2 cards clipped off-screen with no scroll) — flexbox-column `min-height: 0` was missing on PlayerSetup `styles.panel`, latent since v2.69.x but only surfaced when v3.0.16's inner-card `flex: '1 1 360px'` made cards tall enough to overflow. v3.0.17 ships a card-engine correctness sweep: phase-filtered global `tick_modifier` (10 cards) so "All filing times" only ticks REGULATORY-phase players and "All construction times" only ticks CONSTRUCTION-phase; L003/L048 global-DISCARD fan-out with per-player picker (mirrors v3.0.14 L049 fix but for DISCARD); dormant `revokes_approval` parser bug fixed (CSV column 30 had been read by CardService but never wired through parseCardsCsv since v2.61.1 W7 — silently undefined for ~6 weeks). New `affected_phase` column on CARDS_EXPANDED.csv. New integrity-gate tests caught L004/L011/L018/L022/L049 as over-broadcasters on first run. v3.0.16 unifies the setup screen across PC and TV mode — dropped four `!isTVMode` gates (gear icon, PC/TV toggle, Add Player + Start Game, settings drawer) and deleted the orphan TV-only Win Condition block from v2.69.2. The mode toggle now only forks the *in-game* UI (TVDisplay vs GameLayout), not the setup. Also folded in the v3.0.15 "Phone + Board Readability v2" sprint (drafted but never deployed standalone — 7 v3.0.13 playtest reports closed in one coherent sprint). v3.0.15 history retained: phone keyboard fix via `100dvh`, responsive `PlayerList`, TV action-counter chip + `centerOnCurrent` fitView + horizontal player strip during PLAY + controller indicator + phone vibration; dictionary `TextWithTerms` wrap. v3.0.14 closed two v3.0.13 playtest reports and added the `cardTextMatchesColumns.test.ts` integrity gate that's now caught regressions in two further sprints. — closes the full v3.0.13 PM playtest backlog in one coherent sprint (7 reports across phone UX, TV-mode board real estate, and dictionary cross-references). Phone setup: `100vh → 100dvh` so iOS keyboard no longer hides the input (`fb:4b07b80a`); name input no longer locked to 296px so cards fit a 328px iPhone viewport (`fb:02bb1588`). TV board: action counter chip on the current tile (`fb:44221318`); `centerOnCurrent` prop pans+zooms the React Flow viewport to the current tile + valid-move neighbors on each turn (`fb:9c075c16`); QR sidebar dropped during PLAY, per-player info hoisted into a horizontal top strip so the board reclaims 220px (`fb:9bedb559`); prominent pulsing "📱 Look at your phone, [Name]" / "🖥️ ... on the host device" controller indicator in the TV header (`fb:5ca98777`). Phone vibrates the active player's device on turn-start via existing `haptics.turnNotification()`. Dictionary: term-detail definition + simple summary + strategic impact + instructions now go through `TextWithTerms` so cross-references underline and become clickable jumps (`fb:00d1db0a`) — the one place in the whole app that was still rendering raw strings. v3.0.14 closes two v3.0.13 playtest reports and the testing gap that let them ship: `fb:2fe0db6c` (L049 "Each player draws 1 Expeditor Card" silently no-op'd — fixed via new `isGlobalScope` branch in `CardService` DRAW_CARDS block mirroring the existing time-modifier pattern, plus CSV columns corrected to match the promised behavior) and `fb:291d8076` (REG-DOB-TYPE-SELECT Subsequent deadlocked when `pathChoiceMemory` narrowed `validMoves` to 1 — `MovementService.createMovementChoice` now auto-sets `moveIntent` instead of returning "no choice needed"). New `tests/integration/cardTextMatchesColumns.test.ts` integrity gate caught L027 + L042 on first run (same shape as L049 — `tick_modifier=0` despite description promising "All players' filing times decrease by N days") — both fixed in the same commit. v3.0.0 → v3.0.8 history above retained; the evening sprint shipped five more versions driven by the next wave of dashboard feedback. **v3.0.9** ships a dedicated `LifeEventModal` (red theme, ⚡ icon, "LIFE EVENT" header, shake-on-open) so the 1-in-6 L-card draws stop looking "mixed with the Architect modal" (`fb:dfdeaf1c`) — root cause was on the display side, the AutoActionEvent handler was stomping `setDiceResult` and reusing the generic dice modal with the originating space's header. Now queued via `pendingLifeEvent` and opened after the regular dice modal closes. **v3.0.10** ships board readability v2 — three bundled reports: panel space name now matches the board tile via `display_label_override || shortName()` (`fb:41e35769`); new five-step tile size hierarchy in `computeTileVisualState` (compact 150×60 → validMove 180×90 → hover 200×100 → currentBig 220×120 → expanded 240×130) so current player and valid-move tiles get visible size distinction, not just border treatment (`fb:97fa9c75` first half); `visibleEdges` default-filters to path-taken (dim gray dashed) + outgoing-from-current (solid green) so the noisy ~50-arrow network is gone in gameplay, full network preserved in admin mode (`fb:96317d74`). **v3.0.11** ships the end-game stats panel — replaces the bare "Game completed at: X" with Project Summary (scope/spent/days/turns/score/W-cards) + categorized Fees Paid + Funding Sources mix + Approvals & Construction + collapsible Journey, all from data the game was already tracking (`fb:cc345da9 + fb:3483b37b`). **v3.0.12** finishes the second half of `fb:97fa9c75` per user-supplied patch — center-anchored growth via `transform: translate(-(w-150)/2, -(h-60)/2)` so tiles grow symmetrically instead of pushing right+down only; popover treatment for the click-locked size (heavy drop shadow); BoardLayoutEditor renders a dashed 220×120 buffer ghost around every tile so admins can see in-game footprint. **v3.0.13** ships Project Debrief — new `endGameInsights.ts` runs ~20 rules against the stats and surfaces 3–5 tone-tagged observations (🏆 win / 📝 observe / 💡 lesson) so the end screen is now instructive, not just informational (extends `fb:cc345da9 + fb:3483b37b`). Dashboard housekeeping: 29 code-side-resolved feedback reports flipped to `resolved=true` via PATCH /api/feedback/:id.json, unresolved count 51 → 22.

## Where We Are

The game is live at `https://game.unravelcodes.com`. The Beta regression gates (Ghost Player + test suite + typecheck) are in place and have caught real regressions. Workstream 6 (engine-data separation) closed Apr 29, 2026 with v2.58.0. Voice rewrite Pass 1 shipped May 6 (v2.60.0). Workstream 3 (Living Map) Phase A foundation shipped May 8 (v2.62.0); Phase B shipped across v2.63.0–v2.63.3; **Phase C closed v2.64.0 (2026-05-15)** with A* edge routing via `@jalez/react-flow-smart-edge`. v2.63.5 added `GET /api/public/feedback/open` so the `/start` slash command pulls fresh player feedback at session start. v2.63.6–v2.63.8 (May 15 AM) shipped a voice sweep + card-name source-of-truth consolidation, then restored the Ghost Player strict gate to 50 games × ≥90% wins via cancellation-aware abort + forward-bias bot heuristic. **v2.63.9–v2.64.7 (May 15 PM, 9 versions)** rapid-fire shipped a series of playtester-driven fixes: residual E-card voice leaks in the manual-effect path (TurnService + buttonFormatting prefix-extraction return_ case); the long-deferred Workstream 3 Phase C A* edge routing; PM-DECISION-CHECK self-loop data fix; a ResourceSnapshot-driven BeforeAfterBlock in the result modal showing money/scope/time/card deltas with `↔ N swapped` for replace actions; counting `activeCards` so auto-played funding cards (B/I) show up in the delta; time cost on every space header (`+5 days here · 47 days total`); suppression of redundant "Choose your next destination" rows in modals; and a `visualSummary` split so the modal's Summary block shows NPC narrative only (the auto tone+recap stays in `summary` for TTS).

## What's Open

For active priorities and detailed backlog, see [TODO.md](../../TODO.md).

For per-version history, see [CHANGELOG.md](../../CHANGELOG.md).

For Beta strategy and the v3.0.0 ship criteria, see [BETA_PLAN_V3.md](./BETA_PLAN_V3.md).

### Top-of-mind right now

0. **Workstream 7 — Plan Approval Mechanic** — ✅ COMPLETE (2026-05-16 → 2026-05-17). All 5 phases shipped v2.65.0 → v2.65.4 (foundation + badges + revoke triggers + REG-DOB-FINAL-REVIEW two-stage gate + end-game penalty + modal narration sweep). Original PM-DECISION-CHECK resume-hub bug (`fb:bbc94ec8`) closed as a side effect. See [BETA_PLAN_V3.md → Workstream 7](./BETA_PLAN_V3.md) and CHANGELOG for per-phase detail.
1. **Workstream 3 — Living Map** — ✅ **FULLY RETIRED in v3.0.0** (2026-05-23). Phases A–C shipped earlier (coordinate columns, BoardCanvas, A* edge routing). Phase D: drag-to-save shipped v2.66.0 + reopen-revert fixed v2.69.4 + deploy.sh cp -a nesting race fixed v2.69.7. v3.0.0 deleted `BoardV3.tsx` (879 lines), `boardLayout.ts` (785 lines), `BoardV3.css`, and `tests/utils/boardLayout.test.ts` (721 lines). PHASE_COLORS / shortName / truncate relocated to new `src/utils/boardCommon.ts`. TVDisplay swapped to BoardCanvas. BoardToggle dropped Old/New buttons. Net: -2,400 lines. Tagged v3.0.0 in git.
2. **Voice rewrite Pass 2** — modal copy. The doc has `### Modals fired here` tables for ~50 spaces with per-effect modal text. Blocked on a mapping pass: doc uses human-readable labels ("Take Owner's Money") whereas `ModalConfig.csv` keys by engine effect_action values (`add`, `draw_E`). Worth scripting; the doc parser in `scripts/merge-voice-rewrite.mjs` is a starting point.
3. **Story narrative authoring** — creator-driven content. Tool ready (`scripts/set-narrative.mjs`); rollout pace is whatever the user chooses to author. 5 of ~75 card-effect rows have narratives so far.
4. **Workstream 5 — Live Dictionary** — ✅ **closed v2.67.0** (2026-05-20). CORS allow_origins added on the scraper side, same-origin guard removed on the game side. Live terms now hydrate on every game start; bundled CSV remains as offline-resilience fallback.

## Reading Map

| Question | Doc |
|---|---|
| What did we ship recently? | [CHANGELOG.md](../../CHANGELOG.md) |
| What's the user-facing change? | [docs/user/RELEASE_NOTES.md](../user/RELEASE_NOTES.md) |
| What's the active backlog? | [TODO.md](../../TODO.md) |
| Why does the architecture look this way? | [BETA_PLAN_V3.md](./BETA_PLAN_V3.md), [ARCHITECTURE.md](../technical/ARCHITECTURE.md) |
| How do I run the tests / deploy? | [CLAUDE.md](./CLAUDE.md), [TESTING_GUIDE.md](../technical/TESTING_GUIDE.md) |
| What's the gameplay? | [USER_MANUAL.md](../user/USER_MANUAL.md) |

> This file used to be a rolling log of recently-completed work. That role has been collapsed into CHANGELOG.md (which is per-version) and the "Top-of-mind" list above (which captures pending work). Old per-week status entries are in git history if needed.
