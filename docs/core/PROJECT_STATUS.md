# Project Status

**Last Updated:** May 25, 2026
**Current Phase:** Beta — live in production
**Current Version:** **3.0.15** (pending deploy). v3.0.15 ships "Phone + Board Readability v2" — closes the full v3.0.13 PM playtest backlog in one coherent sprint (7 reports across phone UX, TV-mode board real estate, and dictionary cross-references). Phone setup: `100vh → 100dvh` so iOS keyboard no longer hides the input (`fb:4b07b80a`); name input no longer locked to 296px so cards fit a 328px iPhone viewport (`fb:02bb1588`). TV board: action counter chip on the current tile (`fb:44221318`); `centerOnCurrent` prop pans+zooms the React Flow viewport to the current tile + valid-move neighbors on each turn (`fb:9c075c16`); QR sidebar dropped during PLAY, per-player info hoisted into a horizontal top strip so the board reclaims 220px (`fb:9bedb559`); prominent pulsing "📱 Look at your phone, [Name]" / "🖥️ ... on the host device" controller indicator in the TV header (`fb:5ca98777`). Phone vibrates the active player's device on turn-start via existing `haptics.turnNotification()`. Dictionary: term-detail definition + simple summary + strategic impact + instructions now go through `TextWithTerms` so cross-references underline and become clickable jumps (`fb:00d1db0a`) — the one place in the whole app that was still rendering raw strings. v3.0.14 closes two v3.0.13 playtest reports and the testing gap that let them ship: `fb:2fe0db6c` (L049 "Each player draws 1 Expeditor Card" silently no-op'd — fixed via new `isGlobalScope` branch in `CardService` DRAW_CARDS block mirroring the existing time-modifier pattern, plus CSV columns corrected to match the promised behavior) and `fb:291d8076` (REG-DOB-TYPE-SELECT Subsequent deadlocked when `pathChoiceMemory` narrowed `validMoves` to 1 — `MovementService.createMovementChoice` now auto-sets `moveIntent` instead of returning "no choice needed"). New `tests/integration/cardTextMatchesColumns.test.ts` integrity gate caught L027 + L042 on first run (same shape as L049 — `tick_modifier=0` despite description promising "All players' filing times decrease by N days") — both fixed in the same commit. v3.0.0 → v3.0.8 history above retained; the evening sprint shipped five more versions driven by the next wave of dashboard feedback. **v3.0.9** ships a dedicated `LifeEventModal` (red theme, ⚡ icon, "LIFE EVENT" header, shake-on-open) so the 1-in-6 L-card draws stop looking "mixed with the Architect modal" (`fb:dfdeaf1c`) — root cause was on the display side, the AutoActionEvent handler was stomping `setDiceResult` and reusing the generic dice modal with the originating space's header. Now queued via `pendingLifeEvent` and opened after the regular dice modal closes. **v3.0.10** ships board readability v2 — three bundled reports: panel space name now matches the board tile via `display_label_override || shortName()` (`fb:41e35769`); new five-step tile size hierarchy in `computeTileVisualState` (compact 150×60 → validMove 180×90 → hover 200×100 → currentBig 220×120 → expanded 240×130) so current player and valid-move tiles get visible size distinction, not just border treatment (`fb:97fa9c75` first half); `visibleEdges` default-filters to path-taken (dim gray dashed) + outgoing-from-current (solid green) so the noisy ~50-arrow network is gone in gameplay, full network preserved in admin mode (`fb:96317d74`). **v3.0.11** ships the end-game stats panel — replaces the bare "Game completed at: X" with Project Summary (scope/spent/days/turns/score/W-cards) + categorized Fees Paid + Funding Sources mix + Approvals & Construction + collapsible Journey, all from data the game was already tracking (`fb:cc345da9 + fb:3483b37b`). **v3.0.12** finishes the second half of `fb:97fa9c75` per user-supplied patch — center-anchored growth via `transform: translate(-(w-150)/2, -(h-60)/2)` so tiles grow symmetrically instead of pushing right+down only; popover treatment for the click-locked size (heavy drop shadow); BoardLayoutEditor renders a dashed 220×120 buffer ghost around every tile so admins can see in-game footprint. **v3.0.13** ships Project Debrief — new `endGameInsights.ts` runs ~20 rules against the stats and surfaces 3–5 tone-tagged observations (🏆 win / 📝 observe / 💡 lesson) so the end screen is now instructive, not just informational (extends `fb:cc345da9 + fb:3483b37b`). Dashboard housekeeping: 29 code-side-resolved feedback reports flipped to `resolved=true` via PATCH /api/feedback/:id.json, unresolved count 51 → 22.

## Where We Are

The game is live at `https://game.unravelcodes.com`. The Beta regression gates (Ghost Player + test suite + typecheck) are in place and have caught real regressions. Workstream 6 (engine-data separation) closed Apr 29, 2026 with v2.58.0. Voice rewrite Pass 1 shipped May 6 (v2.60.0). Workstream 3 (Living Map) Phase A foundation shipped May 8 (v2.62.0); Phase B shipped across v2.63.0–v2.63.3; **Phase C closed v2.64.0 (2026-05-15)** with A* edge routing via `@jalez/react-flow-smart-edge`. v2.63.5 added `GET /api/public/feedback/open` so the `/start` slash command pulls fresh player feedback at session start. v2.63.6–v2.63.8 (May 15 AM) shipped a voice sweep + card-name source-of-truth consolidation, then restored the Ghost Player strict gate to 50 games × ≥90% wins via cancellation-aware abort + forward-bias bot heuristic. **v2.63.9–v2.64.7 (May 15 PM, 9 versions)** rapid-fire shipped a series of playtester-driven fixes: residual E-card voice leaks in the manual-effect path (TurnService + buttonFormatting prefix-extraction return_ case); the long-deferred Workstream 3 Phase C A* edge routing; PM-DECISION-CHECK self-loop data fix; a ResourceSnapshot-driven BeforeAfterBlock in the result modal showing money/scope/time/card deltas with `↔ N swapped` for replace actions; counting `activeCards` so auto-played funding cards (B/I) show up in the delta; time cost on every space header (`+5 days here · 47 days total`); suppression of redundant "Choose your next destination" rows in modals; and a `visualSummary` split so the modal's Summary block shows NPC narrative only (the auto tone+recap stays in `summary` for TTS).

## What's Open

For active priorities and detailed backlog, see [TODO.md](../../TODO.md).

For per-version history, see [CHANGELOG.md](../../CHANGELOG.md).

For Beta strategy and the v3.0.0 ship criteria, see [BETA_PLAN_V3.md](./BETA_PLAN_V3.md).

### Top-of-mind right now

0. **Workstream 7 — Plan Approval Mechanic** — ✅ COMPLETE (2026-05-16 → 2026-05-17). All 5 phases shipped v2.65.0 → v2.65.4 (foundation + badges + revoke triggers + REG-DOB-FINAL-REVIEW two-stage gate + end-game penalty + modal narration sweep). Original PM-DECISION-CHECK resume-hub bug (`fb:bbc94ec8`) closed as a side effect. See [BETA_PLAN_V3.md → Workstream 7](./BETA_PLAN_V3.md) and CHANGELOG for per-phase detail.
1. **Workstream 3 — Living Map** — ✅ **FULLY RETIRED in v3.0.0** (2026-05-23). Phases A–C shipped earlier (coordinate columns, BoardCanvas, A* edge routing). Phase D: drag-to-save shipped v2.66.0 + reopen-revert fixed v2.69.4 + deploy.sh cp -a nesting race fixed v2.69.7. v3.0.0 deleted `BoardV3.tsx` (879 lines), `boardLayout.ts` (785 lines), `BoardV3.css`, and `tests/utils/boardLayout.test.ts` (721 lines). PHASE_COLORS / shortName / truncate relocated to new `src/utils/boardCommon.ts`. TVDisplay swapped to BoardCanvas. BoardToggle dropped Old/New buttons. Net: -2,400 lines. Tagged v3.0.0 in git.
2. **Voice rewrite Pass 2** — modal copy. The doc has `### Modals fired here` tables for ~50 spaces with per-effect modal text. Blocked on a mapping pass: doc uses human-readable labels ("Take Owner's Money") whereas `ModalConfig.csv` keys by engine effect_action values (`add`, `draw_E`). Worth scripting; the doc parser in `scripts/merge-voice-rewrite.mjs` is a starting point.
3. **Story narrative authoring** — creator-driven content. Tool ready (`scripts/set-narrative.mjs`); rollout pace is whatever the user chooses to author. 5 of ~75 card-effect rows have narratives so far.
4. **Workstream 5 — Live Dictionary** — ✅ **closed v2.67.0** (2026-05-20). CORS allow_origins added on the scraper side, same-origin guard removed on the game side. Live terms now hydrate on every game start; bundled CSV remains as offline-resilience fallback.

## Reading Map

| Question | Doc |
|---|---|
| What did we ship recently? | [CHANGELOG.md](../../CHANGELOG.md) |
| What's the user-facing change? | [docs/user/RELEASE_NOTES.md](../user/RELEASE_NOTES.md) |
| What's the active backlog? | [TODO.md](../../TODO.md) |
| Why does the architecture look this way? | [BETA_PLAN_V3.md](./BETA_PLAN_V3.md), [ARCHITECTURE.md](../technical/ARCHITECTURE.md) |
| How do I run the tests / deploy? | [CLAUDE.md](./CLAUDE.md), [TESTING_GUIDE.md](../technical/TESTING_GUIDE.md) |
| What's the gameplay? | [USER_MANUAL.md](../user/USER_MANUAL.md) |

> This file used to be a rolling log of recently-completed work. That role has been collapsed into CHANGELOG.md (which is per-version) and the "Top-of-mind" list above (which captures pending work). Old per-week status entries are in git history if needed.
