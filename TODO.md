# TODO - Game Alpha

**Last Updated:** September 4, 2026 — v3.2.44–v3.2.50 since the last header update. Highlights: the opening-space trap ("Lock the scope" had no name on screen until you'd already done the right thing, v3.2.45); "Return Expeditor" renamed to "Let one expeditor go" because it is a layoff, not a return (v3.2.47); and writing to the log no longer changes what the dice do — log-entry ids were minting from the same `Math.random` the dice roll from (v3.2.48). Board tiles finally have distinct plain-English names — 12 of 27 shared a label with another tile because `shortName()` strips the NPC prefix (v3.2.50). Full detail: CHANGELOG v3.2.44–50.
**Status:** Beta — **v3.2.50 deployed and confirmed live** (`/health` → `b4765b5`, 2026-09-04). Nothing is awaiting deploy.
**Current Version:** 3.2.50. 3.2.46 was skipped in numbering and the commit that claimed it shipped as 3.2.49.

---

## 📌 Documentation rule — the slimness contract (tightened 2026-07-10)

- **✅ Completed** → move to `CHANGELOG.md` and **delete** from here (don't just check off).
- **Section preambles: ONE pointer line max** (e.g. "history: CHANGELOG v3.0.91–98"). Never re-narrate shipped work here — that's CHANGELOG's job. (2026-07-10: preamble recaps had regrown this file to 306 lines / ~26K tokens, past the read cap.)
- **Trigger-gated / deferred / "revisit if noticed" items** live in the 🅿️ Parking lot at the bottom, not in active sections.
- **Size guard:** active portion (above the Parking lot) > ~150 lines, or whole file > ~250 → trim during `/koniec` step 4.

---

## 🔎 Active — bugs & investigations

*2026-07-11 blind code review: all 10 items fixed — history in CHANGELOG v3.0.112–120. `npm test` "hang" root-caused + fixed v3.1.11. 2026-07-21 playtest findings (6 card-effect gaps): all resolved — see CHANGELOG v3.1.45–v3.1.54.*

> **⚠️ Playtest findings dated before 2026-09-03 may describe the HARNESS, not the game.** Until `b6ef5cd` (Jarvis repo, `scripts/game_playtest.py`) the playtest bot found every clickable control with a single selector, `.uc-hint-glow` — which in `PlayerPanelV2` is a *first-visit hint animation* (`firstVisitHint = isMyTurn && player.visitType === 'First'`), so it renders on nothing from the second visit to a space onward; the commit control was gated harder still (`firstVisitHint && commit.ready`). A bot that cannot see the other controls will press the one it can see and then explain itself fluently — **the least trustworthy findings are "options listed but nothing works" and repeated looping.** What SURVIVES: anything verified in source (the v3.2.45 forward-label defect), and anything measured off the server's `globalActionLog` instead of the bot's own account (the 11 → 0 below). What WEAKENS: the bot's narrative of *why* — including the "11 of its 17 moves" story further down, which is now suggestive rather than evidence. General rule, worth more than this instance: **establish an instrument's field of view before believing its output, and prefer the system's own records to the tester's self-report.**

- [ ] **Con-Initiation "Determine Outcome" (Quality + Multiplier contractor dice roll) may crash the whole game panel — reproduced twice in live playtesting 2026-08-15, needs confirmation in a normal foregrounded browser before treating as certain.** Both times, pressing "Determine Outcome" at Con-Initiation/First ("Sit down, let's talk price") made the entire main game UI vanish — board, player panel, all action buttons gone, leaving only the persistent footer bar and the bug-report button. Didn't self-recover (waited 3s+ both times). Investigated the code looking for a cause: `EffectFactory.ts`'s dice-roll value parsing has solid early-return guards against empty/malformed rolls before reaching the Quality/Multiplier branch, and the app's `ErrorBoundary` (`src/components/common/ErrorBoundary.tsx`) — which would show a "⚠️ Something went wrong" fallback screen and unconditionally `console.error` — never triggered; no matching error appeared in the console either. **Important caveat:** this session's browser automation ran the tab backgrounded/headless, which caused several OTHER false-positive-looking issues earlier in the same session (Framer Motion exit animations stalling because `requestAnimationFrame` is throttled on hidden tabs, producing "stuck" buttons that were actually fine) — so this could be a genuine crash, or it could be another artifact of that same environment constraint. **Action needed:** reproduce directly — reach Con-Initiation (Owner → any funding path → PM Check → "Hunting for an architect" → through Design/Regulatory → Con-Initiation), press "Determine Outcome," and check DevTools console for the real error. Also relevant: there's no way to resume/rejoin a crashed game after a page refresh (see the item below), so once it happens the session is a dead end — can't be dug into further after the fact. **2026-08-15 stats revisit (post-dashboard-ship `visitors.log` pull): no signal either way — a UI crash never fires a `PLAYTEST_*` tracking event, so the log can't confirm or rule this out. Weak circumstantial note only: this same day's later automated test playthroughs (`G-NUAJ-DCF2`, `G-ZFVH-6XEE`) both reached and cleared Con-Initiation without incident and fired `PLAYTEST_GAME_FINISHED` — consistent with, not proof of, the headless-tab-artifact theory above. Still needs the direct foregrounded-browser repro.** **2026-08-17 repro attempt via the session's Browser-pane tool hit the identical wall before even reaching Con-Initiation: `document.visibilityState` reported `'hidden'` the whole time (confirmed via direct JS eval), and the very first dismiss-modal button (the Owner's opening narration "Continue") never visibly closed despite the click firing real network requests (`POST /api/games/.../state` → 200) underneath it — same Framer-Motion-exit-stuck-on-a-hidden-tab shape as before. This tool cannot produce a trustworthy repro of this bug; it needs Tom's own physical browser, foregrounded, tab actually visible.
- [ ] **Engagement data / D&D-skin decision — revisited 2026-08-15 per the 2026-08-14 flag below, using `server/engagementStats.js` run directly against `visitors.log`. Its verdict was RETRACTED 2026-09-01 — see the end of this item; the instrument cannot tell the maintainer's own testing from real players.** Of 15 `GAME_STARTED` events since tracking went live (v3.1.89, 2026-08-02), 6 are this project's own Claude/agent test sessions (`Claude/...Electron/...` UA — filtered out, not real players). Of the 9 real games: **4 are single-player and never get a second player** — `CREATE_GAME` → exactly one `PLAYER_JOINED` → one `PLAYTEST_SPACE_REACHED` on the very first space (`OWNER-SCOPE-INITIATION`) → nothing further (`G-U7TE-8BE7` 08-03, `G-7YHN-SQRS` 08-07, `G-9VE9-UKYM` 08-13; `G-2M4C-DMNM` 08-10 came back 9h later to open the rules/glossary panels but still never took a turn or got a 2nd player). 1 real game (`G-BBY2-ZN6Y`, 08-09) played all the way through — 16 unique spaces including Con-Initiation, fired `PLAYTEST_GAME_FINISHED`. 2 more made real multi-space progress before going idle (`G-EFJB-TEHY` 08-02, 3 spaces/2.7min; `G-MA65-WJCN` today, 5 spaces/28.6min, still inside the abandon window). No "stuck mid-board" pattern turned up anywhere — every real stall is at space #1, always solo, never a confused player wandering later spaces. ~~**Verdict: the recurring blocker is a game never getting a second player.**~~ **⚠️ RETRACTED 2026-09-01 — do not cite this.** Maintainer, asked directly: *"the games are usually one player because i usually have one person testing them out. hence there are no other players."* The solo games are his own testing, not strangers abandoning at the lobby. The filter excludes only the Claude/agent UA (`Electron`/`MSIX`); it has **no way to exclude his own manual sessions from a real browser or the TV**, so his testing is counted as real player traffic. "4 of 9 never got a second player" is therefore un-interpretable, not a finding — and both conclusions drawn from it fall (join-friction is not established, and the D&D hold rests on nothing measured). **Prerequisite before this dataset can answer anything: make sessions attributable** — a maintainer/test flag at game creation, or exclude his home IP the way the foreign-game alert already recognises it. Standing caution: this is the second time this dataset produced a confident verdict that dissolved once we knew who was at the keyboard.
- [ ] **ARCH-FEE-REVIEW is the next Try Again trap, and it costs 50 days a press.** Found 2026-09-03 by the first playtest run against deployed v3.2.45, verified off the server (`G-5RJ3-XFQW`). **v3.2.45 worked**: Try Agains at `OWNER-SCOPE-INITIATION` went **11 → 0**, and committed moves went from 16 across six games (2.7/game) to **7 in a single game**. The bot walked seven spaces deep instead of dying on the first — then burned **5 Try Agains at ARCH-FEE-REVIEW, 50 days each, 250 of its 272 total days on one space.** The 50-day cost is intended (`SPACE_EFFECTS.csv:57`, First visit; Subsequent is 15). The problem is that repeating it is game-ending and nothing conveys the scale before you commit.
  - **This is new evidence for a question closed too early.** On 2026-09-02 the "Push back never says what it costs" finding was withdrawn — correctly, on the facts then: `startHold` reveals the cost bubble on pointer-down, before the 650ms hold commits. That reasoning holds at a **1-day** space, where being wrong is cheap. It does not obviously hold at a **50-day** one, where five presses end the run. Worth re-weighing whether a permanent cost line under the Try Again caption earns its place at high-cost spaces specifically — not a blanket reversal of the earlier call. **Direct evidence added 2026-09-03 (harness run 6, against live v3.2.47):** the bot walked into the 50-day space twice and **its stated per-move reasoning never mentions a cost at all.** It is not weighing 50 days and accepting them; it does not appear to see them. That is evidence about *disclosure*, not about balance — the cost bubble exists but only on press, and a first-timer has no reason to press a button they are still deciding about.
  - Caveats from the harness session, worth keeping honest: that was **one** game, not six, and it still ended on "gave up after 3 clicks" (at this space, not the opening one). No bot has yet completed a full game.
  - **A second Try Again loop is reachable at OWNER-DECISION-REVIEW** — a bot pressing Try Again out to 4,259 days. Seen once, on 2026-09-03, in the one re-dealt smart-bot batch that the RNG coupling produced (CHANGELOG v3.2.48); it is NOT reachable in the current seeded baseline, so nothing in the gate watches for it. The loop itself was always there — that shuffle just walked into it. Same shape as the trap v3.2.45 fixed in the UI.

- [ ] **Jarvis local-model playtest 2026-09-02 — ONE item left; three of the four I first listed were wrong and are withdrawn.** Report: `E:\Downloads\playtest20260902.txt`, also `~/.hermes/playtest-reports/playtest-2026-09-02.txt` on the Mac — first run that captured per-move *reasoning*, which is what made the real finding legible. A 9B local model spent **11 of its 17 moves** alternating between the two action buttons and **Push back** at OWNER-SCOPE-INITIATION because the commit spine read "2 actions left" and *nothing on screen was named "Lock the scope"*. **Fixed in v3.2.45.** Only this remains:
  - **Does a pending CARD_REPLACEMENT choice disable its own action button?** At PM-DECISION-CHECK the bot pressed "Replace Expeditor" three times and gave up ("3 clicks that would not land"). With 3 E cards and `replaceCount` 1, `CardEffectService.handleReplaceCards` opens a choice modal; `ManualActionProcessor` has no `awaitingChoice` guard. Almost certainly the modal blocks the pointer and this is a harness artifact — **one foregrounded-browser check settles it**: open the modal, try to press the button behind it. If it's blocked, delete this item.

  *Withdrawn after maintainer review 2026-09-02 (he was right on all three, verified in code):* **(a)** "Push back never says what it costs" — `startHold` calls `revealBubble()` on pointer DOWN, so the cost bubble ("+1 day", "will be re-drawn next turn") shows on any press, before the 650ms hold commits anything. **(b)** "Finished actions look like live buttons" — `doneActionRow` is already muted text on a dimmer ground with `cursor:default`; the identical footprint is deliberate (fb:44df6d5d). **(c)** "Put glossary links on the action buttons" — actively harmful: `TextWithTerms` renders `<span role="button">` with `stopPropagation()`, so a glossary term inside a real `<button>` would swallow the click and the action would never fire. Bare jargon on buttons is still a gap, but it belongs to the "Decide who the game is FOR" fork below, and any fix must sit *beside* the label, never inside it.

  *Standing lesson: this model can only act on what the screen literally says at that instant, which makes it a good detector of missing words and a bad detector of visual state (grey vs. live, modal vs. panel). Weigh its findings accordingly — and check the code before writing one up.*

- [ ] **Expeditor pick-with-tradeoffs — the reporter already designed it, we just could not see it.** Surfaced 2026-09-01 when the `extra` field first reached triage. fb:f22035af's `extra` carries a concrete proposal the one-line summary never showed: expeditors with real strengths and weaknesses instead of "hire 3" — *"one is cheap but slow, one is fast but high fee, one is great with Plan Exam but weak at Prof Cert"* — plus later events where an expeditor underperforms or leaves and the player has to adapt. The Parking-lot entry for this says only "bigger design change"; that is now an under-description. Re-read the report before scoping. <!-- fb:feedback-1778584030168-f22035af -->
- [ ] **Completed actions should promote Negotiate/Accept to the highlighted buttons** — a SECOND, separate request hidden in fb:ae480630, surfaced 2026-09-01 by the `extra` fix. Verbatim: *"also when actions are completed the negotiate and/or accept buttons should become the highlighted buttons."* This is not the disappearing-highlight bug that report has been chased on since July — that one is about a highlight going stale, this is about which control earns the highlight once a space's actions are done. Never tracked, never built. Worth checking whether it also explains part of the original "I wanted to know what to press next" complaint. <!-- fb:feedback-1784464219688-ae480630 -->
*Workstream 6 CSV-only-reskin audit (2026-08-03) — closed 2026-08-14, every finding fixed: DOB end-game penalty verified safe (v3.1.96), Investor/Investment label + CON-INITIATION fee-category bugs (v3.1.98–99), DOB/FDNY narration + CHARACTER_MAP (v3.1.101–102), the two BLOCKING findings — card mechanics gated on literal IDs, Homeowner Violation hardcoded numbers (v3.1.103–104), `REG_PREFIXES` space-name-prefix filter (v3.1.105), and the full `uiStrings.ts`/`RulesModal.tsx` vocabulary-swap reskin lift (v3.2.1–v3.2.2). Full history in CHANGELOG; nothing left open from this audit.*

## 🎓 Active — onboarding (the audience fork is ANSWERED, 2026-09-03)

- [ ] **Onboarding Phase C: plain-English aliases** (this line used to propose a NEW `display_label_plain` column — superseded 2026-09-04: the existing `display_label_override` in GAME_CONFIG already does the job for tiles, so no schema change is needed; buttons still need a home) — Phase A shipped v2.70.5. **Unblocked 2026-09-03 — the maintainer answered the fork: the game is for BROADER PLAYERS / beginners, NOT insiders.** This item had been silently assuming that answer; it is now the recorded decision. Chosen direction is the second branch of fb:8ad42b52 in full — a real tutorial, tooltips, and story-based micro-lessons that explain each term in one or two plain sentences — and explicitly NOT the first branch (keep the jargon, lean into insider edge-case events). The evidence that forced the fork also names the first term to teach: harness run 6 against live v3.2.47 showed the "Let one expeditor go" rewording **worked** (the old *"'returning' suggests giving it back"* confusion is gone entirely) and surfaced what sat underneath it, 8 hits of *"the term 'expeditor' is unclear jargon, and the benefit of letting one go is unknown."* Rewording one term at a time kept exposing the same gap — that is what made this a fork, not polish. **Started 2026-09-04 — the TILE-LABEL half shipped as v3.2.50**, and no new column was needed: `display_label_override` already exists in GAME_CONFIG, already wins over `shortName()` in the render path, and now carries a plain-English label for all 27 spaces (the maintainer's own approved strings). That also closed a defect nobody had logged: 12 of 27 tiles shared a label with another tile, because `shortName()` strips the NPC prefix and collapsed four spaces to "Fee Review", three to "Scope Check", three to "Initiation", two to "Plan Exam". A uniqueness guard over the real CSV now lives in `tests/utils/boardCommon.test.ts`. **The BUTTON-LABEL half shipped as v3.2.51** — again no new column was needed: `w_/b_/i_/e_card_label` in `Spaces.csv` and `button_label` in `DiceRoll Info.csv` already existed and were **0% populated**, so all 80 player-facing buttons fell through to the generated wording in `buttonFormatting.ts` (the same shape as the tiles). 81 labels authored to the maintainer's rule: *"buttons say what you're doing, in everyday words. No trade jargon on buttons"* — which took "expeditor" off every button including `return_e`, since v3.2.47's "Let one expeditor go" fixed the direction problem but left 8 hits of "I don't know what an expeditor is". Deciding reason is structural: a glossary link can never sit inside a button, so a button is the one surface where a hard word cannot explain itself. Guarded by `tests/utils/buttonLabels.test.ts`. **Still open:** the tutorial / tooltips / micro-lessons that are the bulk of this item. **Explicitly NOT in this item:** jargon inside story prose (expeditor, standpipe, egress) — that is the glossary's job. One constraint for whoever builds the tooltips: a glossary term must never be placed inside an action BUTTON — `TextWithTerms` renders `<span role="button">` with `stopPropagation()`, so a term inside a real `<button>` swallows the click and the action never fires (settled finding, previously withdrawn and re-confirmed). <!-- fb:feedback-1778583921001-0aa9660c --> <!-- fb:feedback-1778584099671-8ad42b52 -->

## 🔒 **Security audit follow-ups** (2026-07-21, MIXED priority)

*Source: security audit of the repo, 2026-07-21. The CRITICAL findings (join-info token leak, XSS via player name, path-traversal on classroom `:id` routes) were fixed the same day — see CHANGELOG v3.1.21 — and are not duplicated here. The audit's INFORMATIONAL and clean-audit items (CSRF n/a since no cookies, no SQL injection surface, scrypt password hashing, secrets not in shipped bundle, etc.) confirmed working correctly and need no action.*

### HIGH — external action (not a code fix)
*(PixelLab.ai API key rotated 2026-07-31, see CHANGELOG — old leaked key is now dead. New key lives in `.env`, gitignored, not read by the running server. Git-history purge of the old key: maintainer decided 2026-08-02 to leave it — not worth the disruption for a dead key.)*

### LOW — polish
*(CSP/HSTS/Permissions-Policy headers shipped v3.1.78, see CHANGELOG — full external-resource survey + live-browser verification, not just headers added blind.)*

## 📱 Active — playtester acquisition (PRD phases 1–2 shipped; history: CHANGELOG v3.0.95–97)

*Spec: [Unravel_Codes_Playtester_Acquisition_PRD_v4_Lean.docx](Mockups/Unravel_Codes_Playtester_Acquisition_PRD_v4_Lean.docx). Screenshot carousel finished v3.1.85, see CHANGELOG.*
- [ ] **Demo video** — full creative brief recovered from Downloads 2026-08-21 and saved to [Mockups/Trailer_Creative_Brief_v4.1.md](Mockups/Trailer_Creative_Brief_v4.1.md) (was previously only "in chat," now tracked); 50–55s target, not 30–45s as this line previously said. Immediate actions per the brief: approve v4.1 storyboard, test the hook animatic, cast + rehearse, record custom SFX — then film, edit/A-B test, deliver final cuts (60s/30s/15s/vertical), wire the "Watch demo" button.

## 🚀 Active — infra / deploy / data

- [ ] **G160 board-editing suite — needs the maintainer's own real-browser confirmation, now covering two rounds of unverified work (v3.1.95 and v3.2.15).** Real-browser testing found the drag genuinely didn't work at first (missing `nodrag`/`nopan` was a red herring — the actual cause was the maintainer testing through the *Board Layout Editor*, a separate tool G160 was never wired into). Once fixed, live testing drove real feature growth: redirects made permanent/server-backed (matching tile positions, not per-device localStorage), multi-bend waypoints scaled by connector length, manual bundling (drag two connectors' points together to merge them, snap-apart to un-merge), box-side anchor snapping (4 fixed points per tile), and a per-connector restore picker (the old "restore" buttons cleared everything at once) plus a distinct cursor + hover-highlight so overlapping handles are easier to tell apart (v3.1.90–v3.1.95). Two real bugs surfaced and fixed via the maintainer's own testing along the way: v3.1.92 (old single-point waypoint data crashed the new array-based code) and v3.1.94 (anchor handles landed on the exact same pixel when several connectors pinned to one tile side, and the Board Layout Editor turned out to have no way to hide a connector at all). Closes fb:feedback-1778327469678-d27a73d0 ("more control of which lines shown and how they run") — confirmed deployed live (`/health` → `8af3a93`); maintainer wants to test it live before flipping resolved (asked 2026-08-04, answer: "i need to test it") — leave open until confirmed.

  **2026-08-19 (v3.2.15) added a second, larger unconfirmed batch from live playtesting feedback:** connector labels now prefix each side with its NPC discipline instead of a bare space name (two different edges could both read "Fee Review → Scope Check" — phase alone doesn't disambiguate Architect vs Engineer, both DESIGN phase); the restore-picker list is now alphabetized instead of hide-order; self-loop connectors (a dice space rolling "stay here") are no longer drawn; hovering a handle with only one *visible* line no longer opens the "N connectors here" picker because a hidden sibling was still counting; connector lines take their source space's phase color; the editor's spacing-guide ghost now carries the same drop-shadow the real "current" tile gets, so gaps that looked clean in the editor won't visually overlap once a tile goes current in real play. **None of this was seen live this session** — the automated browser environment renders zero React Flow edges at all (confirmed via a git-stash A/B test to be a pre-existing tooling limitation, not something this session broke), so all of it was verified via unit tests + code review only. Also fixed along the way: a compound dice outcome like `"A or B or C"` wasn't being split for the board display, silently dropping several of REG-FDNY-PLAN-EXAM's connector lines.
- [ ] **Card library / space-editor consolidation — design approved 2026-08-21, spec: [CARD_LIBRARY_DESIGN.md](docs/core/CARD_LIBRARY_DESIGN.md).** Merges the two overlapping space editors (⚙️ Space Data Editor + 🏫 Classroom Setup) into one rolodex of cards with official/school/teacher ownership; editing branches instead of overwriting. Build order in the spec: (1) stop losing work — ~~cards gain an `owner` field~~ and ~~fix the Board Layout Editor's wrong help text~~ both DONE v3.2.18; ~~route Space Data Editor saves into the card library~~ **STAGE 1 COMPLETE v3.2.22** — editor saves now survive restarts (verified end-to-end: edit posted, server restarted, edit still served; it lives in the classroom card, not in stock). Stage 1b (v3.2.20+v3.2.21) gave a card its dice, modal copy and logic wording first, so no half-durable saves; (2) rolodex — unselect-not-delete + per-slot card picker + role note + per-card drift badge DONE v3.2.23; branch-on-edit + config backups DONE v3.2.24 (granularity settled by research: a save branches only when content actually differs, 5 versions kept per space, older behind a link; the Phase 1 config-backup promise from TEACHER_LAYER_DESIGN shipped at the same time). **Stage 2 COMPLETE.**; (3) one screen — ~~fix the live preview~~ DONE v3.2.25 (now mirrors PlayerPanelV2, retired stylesheet deleted); merging the editors DONE v3.2.26+v3.2.27 — one Admin Tools button, browse (deck + player view + facts) and editing (deck collapses to a rolodex drawer, versions full-width) in one screen. **Follow-ups: ~~(i) REGRESSION v3.2.29 — a teacher can no longer change what a space says~~ FIXED v3.2.41 (maintainer decided 2026-08-25 to skip the group/school tier: a teacher writes an `individual` card over six wording columns, rebuilt from their own baked board; an edit on an official card branches instead of overwriting. Verified end-to-end against a running server with a real teacher account — see CHANGELOG). (ii) the teacher path still opens the old Classroom Setup, so teachers get the deck without the player view; (iii) DataEditor is unreachable but deliberately left as a fallback — delete once the merged screen is confirmed in real use; (iv) POST /copies and PATCH /copies/:copyId still work and are tested but have no caller in the app.**; (4) group tier + roles. Approval queue deliberately deferred (breadcrumbs in the spec). Tiers are stored structurally (`official`/`group`/`individual`) and labelled through the existing UI_STRINGS swap, so a reskin renames them without a data migration — school/teacher by default, club/master under the D&D skin (maintainer, 2026-08-21). **Stage 1 is the load-bearing one** — today every Space Data Editor edit is silently reverted by the next deploy or restart. Maintainer decided 2026-08-21 that a card owns its slot dice outcomes (visit_type + roll_group carry first/return, one-roll-many-effects, and multi-roll with no new modeling), so a new **stage 1b** (card carries its dice rows, merged at bake incl. the curated DICE_OUTCOMES.csv) must land before the editor save path is swapped. Full data-file audit done 2026-08-21 and settled by a standing rule: a card owns rows keyed to exactly one space holding that space authored content, so it also carries ModalConfig rows and logic-question WORDING (routing stays stock), while PATH_CHOICE_RULES (cross-space) and ACTION_TOOLTIPS (global) stay out.


- [ ] **Confirm the rebuilt preview/editor link with a real click** (v3.2.34, v3.2.37, + the 2026-08-30 fix below). **Tab order: CONFIRMED GOOD** by the maintainer 2026-08-30 — that half is closed. The click half found a real bug, now fixed but **not yet confirmed by a real click**, which is the same wall as ever: the Browser pane never holds keyboard focus (`document.hasFocus()` is false, so `.focus()` moves `activeElement` while firing **zero** focus events — measured, 0 listeners), and `editingRegion` is driven entirely by focus events. Unit tests drive it through the real React path by passing the prop; nothing here can produce the actual focus event.

  **What the maintainer found, 2026-08-30:** *"it displays only if it already exists. if the item is currently blank it does not open up a new area to show. and it does not expand the view if it was colappsed."* Two separate causes, both fixed: (a) the preview draws what a **player** would see, so an unset action or an unraised pop-up was not drawn at all and had nothing to light — worst at exactly the moment you most need telling what a blank field feeds; the region being edited now gets a dashed placeholder slot, and **only** that one, since permanently ghosting all five unset actions is the crowding v3.2.35 was told off for. (b) both fold-outs start shut, so a field feeding one lit its little pencil under the fold; the section now opens itself, adjusted during render rather than in an effect so its own toggle still closes it afterwards. 11 new tests (9 fail against the old panel; 2 are no-regression guards that must pass both ways). **Still worth your eyes:** whether the dashed ghost reads as a placeholder rather than as a real action, which no test can judge.

  Also added: `Element.prototype.scrollIntoView` is now stubbed in `tests/vitest.setup.ts`. jsdom implements no scrolling, so it was simply absent and any test that set `editingRegion` threw inside the effect — which is why this whole code path had never been exercised by a test before.


- [ ] **The Dockerfile ships devDependencies into the running image** — found 2026-08-23 clearing the extract-zip advisories (v3.2.36). The build is single-stage: `npm ci` installs dev tooling and nothing prunes it before `CMD ["node", "server/server.js"]`, so every dev dependency rides along in the production image. Would also shrink the image noticeably. **Do NOT just add `npm prune --omit=dev` — that was tried and audited in v3.2.37 and it is a live outage.** `express`, `cors` and `ws` were declared as *devDependencies* while build-time-only frontend libs sat in `dependencies`; pruning would have deleted Express from the image and taken the server down on the next deploy. Those three were moved to `dependencies` in v3.2.37, so the prerequisite is now done — but the prune itself still needs a real container build to prove, which the session environment cannot do. Remaining work: verify by building the image and starting it before deploying, and consider also moving the genuinely build-only libs out of `dependencies` so the prune actually saves something.
- [ ] **Glossary auto-sync: pending terms are sitting in Purgatory awaiting your approval — count disputed, check the dashboard before acting.** This line said **6** since 2026-07-26; the manager session reported 2026-09-04 that five were approved long ago and only **Crowdfunding** remains, verified against the live glossary. That could not be re-verified from this session — `GET /api/candidates` on the dictionary API is auth-gated (`{"detail":"Not authenticated"}`), so treat **1** as the likely number and the dashboard as the authority. The autosync job itself is healthy: `docker logs -t dictionary-scraper-backend` shows scheduled runs through 2026-09-03 ("no new terms found"), so a low pending count is the expected steady state, not a broken feed. WORKING as of 2026-07-26 22:20 EDT — first successful run staged Standpipe, Heat Recovery Ventilation, Stormwater Management, Tax Credit, Crowdfunding, Environmental Review (tagged `source=game_auto | AI Generated`). Verified from timestamped logs, see CHANGELOG v3.1.62 "Ops". Only action left is a human one: review/approve them on the dashboard's candidates page. Re-check health any time with `ssh unraid "docker logs -t dictionary-scraper-backend | grep glossary-autosync | tail -20"` (`-t` matters). Details: memory [[project_glossary_autosync]].

## 📱 Active — external testing & release

- [ ] **Recruit 3–5 external players** for a structured UAT pass (open since April — the `/challenge` funnel + QR codes above are the remaining enablers).

## 🙋 Decisions waiting on the user
- [ ] **`formatDiceRollButton` is dead code — delete it?** 74 lines in `src/utils/buttonFormatting.ts`, **zero call sites** anywhere in `src/` (only its own definition and a comment in `uiStrings.ts` reference it). Six `DICE_BUTTON` keys are reachable ONLY through it and are therefore dead too: `FUNDING`, `EFFECTS`, `BONUS`, `BONUS_FUNDING`, `BONUS_EFFECTS`, `NEXT_STEP`. Found 2026-09-04 during the v3.2.51 button-label pass; surfaced rather than folded into a wording change.
- [ ] **One malformed row in `SOURCE_FILES/DiceRoll Info.csv`** — blank `space_name`, `visit_type` of `roll_group`, and `die_roll` set to the literal string `button_label`. A header fragment that got into the data. It renders no button and breaks nothing, but it makes every dice-row count read 47 instead of 46. Delete next time that file is open.


### Dashboard PATCH recipe (for flips after a deploy is confirmed live)
`PATCH https://game.unravelcodes.com/api/feedback/<full-id>.json?token=<FEEDBACK_TOKEN>` body `{"resolved":true}` (`.json` suffix required, token-gated). 2026-07-10: 20 fixed-and-deployed reports flipped, 53→33 open. 2026-07-10/11: 5 more flipped (fixloop closures + 2 maintainer-confirmed-already-resolved) → 27 open. 2026-07-12: 3 more flipped after v3.0.115 deploy confirmed (`b46cb30`) → 24 open. 2026-07-13: 6 more flipped after v3.0.121–127 deploy confirmed → 26 open (2 new reports arrived same window). 2026-07-14: v3.0.116–137 deploy confirmed (`c6e516c`) — 2 more flipped (fb:f453b1f3, fb:7dbc2fcc) → 28 open (new reports arrived in the interim). 2026-07-15: v3.0.141 confirmed deployed (`8b5bf45`) but v3.0.142 (the fix that actually closes the TV-fit thread) is NOT yet deployed — 4 reports pending flip once it is: fb:3f9f2831, fb:e121c34e (original "resolution is crap"/"can't go back up"), plus two filed same day from the same real TV, fb:dca292b8 ("TV detected as phone") and fb:28512320 ("still can not see all four players"). Full detail: NEXT_SESSION.md "Flip after deploy". 2026-07-16: v3.0.142 confirmed deployed (`aa80c0b`) — the 4 pending reports above flipped → 29 open (2 new reports arrived in the interim: fb:2b5b9f2a "zoom changes on space change", fb:daf6b7fc "room for three or four colors"). Same day: both new reports FIXED in v3.1.2 (TV pan-only camera + PC zoom-memory clobber; 3 direct-pick color dots). 2026-07-16 (later): v3.1.0–3.1.3 confirmed deployed (`c4471b3`) — fb:2b5b9f2a and fb:daf6b7fc flipped → 27 open. Real-TV confirmation of the camera feel (zoom should stay put between moves) is still outstanding — this session's browser can't play animated camera transitions at all (see CLAUDE.md TACTICAL), so the fix was verified via unit tests + state instrumentation, not a watched animation. 2026-07-18: live re-fetch found 28 open (large gap since 07-16 — a 2026-07-01 feedback-staging file had never been applied to TODO.md; see CHANGELOG "reconcile TODO.md against live dashboard"). Investigating the recovered batch found 16 of them were already fixed in v3.0.91/v3.0.97 (2026-07-01/06, long since deployed) but never flipped — flipped all 16 → 12 open. A 17th (fb:7441e00b) confirmed the same way next day → flipped → 11 open. 2026-07-19: v3.1.20 confirmed deployed (`47c88a7`, bundle-verified) — the 4 pending reports fixed this session (fb:3a5280d8, fb:44751a06, fb:d6bbcb00, fb:2c848b47) flipped → **7 open**. 2026-07-27: v3.1.61 confirmed deployed (`ab5a1bb`) — live re-fetch found 13 open (6 new arrivals since 07-19 in the interim); the 3 TCL-smart-TV reports fixed in v3.1.22 (fb:6c05e9d3, fb:0dcb1053, fb:48d9125d) had never been flipped, flipped now → **10 open**. Same day: v3.1.62 deployed (`2f46a15`) closing the player-panel box-in-a-box report — fb:101702ed flipped → **9 open**. Same day again: v3.1.66 deployed (covering v3.1.64's TV-mode glossary fix) — fb:89d83c39 flipped → 8 open; then fb:b39ea2bd flipped after the maintainer reviewed the measurement showing v3.1.22 had already resolved it (see CHANGELOG v3.1.66 "Investigation") → **7 open**. Of those 7, only fb:ae480630 is recent (probable fix shipped in v3.1.63 but never reproduced — deliberately left open); the other 6 are long-tracked Parking-lot items. 2026-07-31: v3.1.80 confirmed deployed (`5eba257`, bundle-verified: live `index-*.js` embeds `"3.1.80"`) — fb:89d83c39 (re-flipped; live re-fetch this session found it still open despite the 2026-07-27 ledger entry above claiming it was flipped then — the live PATCH today succeeded and it's now confirmed excluded from the open list, so trust this entry over the earlier one) and fb:2948cf19 (clipboard-copy) both flipped → **7 open** (2 new arrivals in the interim keep the count steady: fb:1df093dd, fb:5a155a1a — both already long-tracked Parking-lot items, not new work). 2026-08-02: live re-check found the same 7 open, all already tracked elsewhere in this file — none relate to this session's fixes (those were found via code investigation, not filed feedback).

---

## 🅿️ Parking lot — real but deferred; each fires on its inline trigger

### Product / design (trigger noted per item)
- [ ] **Stand up `dnd.unravelcodes.com` as a second, independent instance for the D&D-skin engagement test (raised 2026-08-03) — ON HOLD. Parked 2026-09-01: the engagement read that justified the hold was retracted (see the engagement-data item), so this is on hold for want of D&D CSV content, not because data said so.** — needs to run the same image against a different CSV dataset, side-by-side with the live game, on its own subdomain. Plan: second container (`game_alpha_dnd`, host port 3081→3001, same `game_alpha` image, own bind-mounted data dir so state/games don't collide with the main instance — `deploy.sh:52-63` and `Dockerfile:56` show `DATA_DIR` is already a runtime-configurable volume, no code change needed for that part). Open decision: where the D&D CSVs themselves live — a second `public/data-dnd/` tree in this same repo picked by a build arg (keeps both datasets versioned together, no branch-drift risk) vs. a separate git branch. Two steps outside this repo, on the user: (1) Cloudflare DNS record for `dnd.unravelcodes.com` mirroring however `game.unravelcodes.com` is set up now, (2) an Nginx Proxy Manager proxy host routing it to the Unraid box's port 3081. Not started — no D&D CSVs exist yet either.
- [ ] **Bug-fix sprint after structured UAT** — placeholder, fires only once a structured UAT pass has actually run; depends on "Recruit 3–5 external players" above.
- [ ] **Board visual evolution as more spaces get added (maintainer idea, 2026-07-14)** — the board may need to represent connections better than a flat 2D layout once it grows; maintainer shared a reference image (abstract particle/bokeh visualization, code panels on the sides) evoking "zoom into one space, its info shows at the edges, you still see the rest." **My recommendation: try this in 2D first, not true 3D.** True 3D (a WebGL engine like Three.js — ReactFlow, the board's actual current renderer, is 2D/DOM-based) is a full re-platforming, not a tweak, and carries real risk specifically for this game: smart TVs (a primary target platform) often have weak/inconsistent WebGL support across Tizen/webOS/Android TV/Fire TV, and "readability over immersion" is the house-standard principle — a 3D re-platform could regress the TV legibility work already shipped for a payoff that isn't guaranteed. The specific desired FEELING (focus on one space, contextual info pinned to the screen edges, everything else still visible but de-emphasized) is a depth-of-field / focus effect, not something that requires true 3D — achievable by extending `BoardNode`'s existing 5-step size hierarchy (`computeTileVisualState` in `boardCommon.ts`) with a new "focus mode": dim/blur non-focused tiles and move the expanded tile's detail content to fixed side panels. Reserve true 3D as a "someday, if we genuinely outgrow 2D legibility" idea.
- [ ] **Real mobile-preview mini-puzzle** — genuine 30–60s phone demo is design work, not engineering; scope separately when there's a clear idea. ("Quick preview" currently routes to the real game as a deliberate A/B placebo.)
- [ ] **Playtest-funnel Phase 3 (optimize on real data)** — trigger: traffic exists. `GET /api/admin/playtest-stats` already aggregates. Metrics: return rate, completion, feedback rate.
- [ ] **This-turn cost line attribution nuance** (prev-turn movement days attribute to current turn) — trigger: playtesters notice.
- [ ] **✓ done-trace tooltip is desktop-hover only** — trigger: players ask for it on touch.
- [ ] **Ledger accordion for remaining areas** ("Where your money's going" + funding-gap line) — trigger: one-screen rule starts fighting again.
- [ ] **Between-turns move overlay sits under full-screen modals** (panel-scoped z-60 vs modal z-1000) — accepted for v1; lift or suppress if wanted.
- [ ] **Change-legibility P2–P5 + audio** — inline tab deltas/flash/badges (P2), tiered work cards + diegetic vocabulary (P3), time-feel duration-mapped transitions + days-proportional burn, NO float model (P4), a11y/pedagogy + debrief (P5), audio = greenfield (no SFX system; visual-equivalent rule). **Reframe against the V2 panel before building — the 2026-06-23 spec is written in classic-panel terms.** Full spec context: this file's git history (2026-06-23 entry) + [player-panel-redesign-20260714.md](docs/archive/player-panel-redesign-20260714.md) §10 (archived 2026-07-18, redesign complete).
- [ ] **Expeditor "presence"** (avatar/causality link-line) — assumes a task-cell grid the data model doesn't have; revisit after P1–P5.
- [ ] **Gantt / schedule today-line view** — trigger: teachers add enough construction-section spaces.
- [ ] **Expeditor mechanic: pick with tradeoffs instead of "hire 3"** — bigger design change. <!-- fb:feedback-1778584030168-f22035af -->
- [ ] **Async/decoupled turn order option** — maintainer's own idea (2026-07-13), explicitly "fun thing to think about for the future," not a request: an optional game-settings mode where players move independently instead of the current coordinated loop, so nobody waits on others — motivated by both faster casual play and eventually repurposing the engine for a D&D-style game where players go off on separate quests. Needs real design work before engineering (shared-card/shared-time mechanics assume a synchronized turn order), not just a settings flag. <!-- fb:feedback-1783919654407-1df093dd -->
- [ ] **Build the "Remote" play mode** (maintainer request 2026-07-14) — a real third mode for players in genuinely separate locations, distinct from both PC ("shared screen") and TV ("shared screen + phone controllers"), which both still assume one physical hub device. The setup-screen button exists now (v3.0.136) as a discoverable "coming soon" placeholder only. Join-by-code + WebSocket state sync already work from anywhere; what's genuinely missing is a mode where NO device is the "shared board" — every connected player needs their own board view.
- [ ] **Log follow-ons:** expandable log rows (chevron → raw detail) + end-screen export + post-game viewer with search/filter — agreed as v3.0.45/46 concepts, never built.
- [ ] **Per-NPC rich sentence templates for dice summary** (~30–40 authored sentences) — trigger: attribution form feels thin in playtest.
- [ ] **`{fundingAmount}` token at remaining spots** — partial 2026-05-29 (BANK/INVESTOR Subsequent done; First-visit + LEND skipped deliberately). Outcome-column adoption needs `spaceOutcome` run through `interpolateTemplate`.
- [ ] **First-game tutorial moment for the approval mechanic** — ties into the onboarding package.
- [ ] **End-game penalty tuning** (`MISSING_DOB_PENALTY_*` in ApprovalService.ts) — trigger: 3–5 real games where players hit the penalty.
- [ ] **OPTIONAL content: author `effects_on_play` prose for 74 E + 49 L cards** — only if a one-line authored effect adds teaching value.

### Reliability / plumbing (trigger noted per item)
- [ ] **HTTP polling fallback while disconnected** — trigger: WS reconnect proves insufficient in real play.
- [ ] **"Start without all phones" override for TV mode** — trigger: the hard-block gets in the way of solo demos.
- [ ] **TV bug-report button pulls phone log buffers** (heartbeat-piggyback ~5s, last ~50 entries/phone) — trigger: next phone-crash-can't-report incident.
- [ ] **Mobile-tab-freeze TEMP-state-loss claim** — external audit theory, unverified; `visibilitychange` reconnect already exists. Trigger: a playtester actually reports lost actions after phone lock.
- [ ] **Deploy stamps commit "unknown" on `docker compose` builds** (Alpine container lacks git) — non-issue while using deploy.sh.
- [ ] **Webhook deployment** (push-to-deploy via HTTP receiver on Unraid) + **verify deploy.sh backup/restore of `game-data/`** — DX wishlist, stalled since April.
- [ ] **`.claude/feedback-staged.md` is gitignored, which is likely why a 2026-07-01 draft (48 candidates) sat forgotten for 2+ weeks** — nothing tracks it, so there's no diff/history to notice it went stale (found + reconciled 2026-07-19, see CHANGELOG). The `/start` skill fix (CHANGELOG cross-check before drafting "new" candidates) prevents the worst symptom, but the file itself staying invisible to git is still a standing risk. Trigger: this kind of silent-drift recurs, or the file's PII-risk (raw player feedback text) is judged acceptable to commit.
- [ ] **No monitoring on the public-facing infra (Cloudflare tunnel, NPM, Tower's containers) — a real outage sat undetected for at least 12 days (found + fixed 2026-08-16).** `cloudflare-tunnel` had a literal unfilled `YOUR_TOKEN_HERE` placeholder and had been crash-looping the whole time, taking `game`/`dashboard`/`api`/`photos.unravelcodes.com` all offline from outside — only noticed because unrelated Docker-daemon work forced a restart that made the failure loud. Nothing currently checks these from outside the house. Trigger: worth a look next time infra changes anyway, or after another silent-outage scare — a simple external uptime check (e.g. a free UptimeRobot/healthchecks.io ping against `https://game.unravelcodes.com/health`) would have caught this in minutes instead of unknown days. Not scoping further unprompted — this is a "worth considering" flag, not a committed build.

### Architecture / code health (bundle these in one dedicated session — same drift-trap shape)
- [ ] **~15 components use full-state `subscribe()`** (PlayerPanelV2/ScoreboardV2 force-rerender on every change) though `subscribeWithSelector` exists. Re-verified 2026-08-02: the domain-event design doc's own rule says don't migrate without a measured render-cost problem (a prior narrower-subscription API was built once and never adopted, same shape) — nothing has changed that overrides that gate. Trigger: a demonstrated render-cost problem, not "would be tidier."
- [ ] **TurnCostLedger full replacement of REAL/TEMP** — considered and declined 2026-07-12 (audited, no bugs from the current overlap, 83-call-site blast radius for zero net simplification). Trigger to revisit: Try Again needs to preserve a 4th+ type of "sticky outflow" beyond money/cards/life-events-drawn.
- [ ] **Three effect pipelines (Space/Dice/Card)** behind one `EffectExecutor` interface — no observed bug; shape worth examining only.
- [ ] **Service decomposition — only on concrete pain signal** (specific method painful to edit, git-blame bug hot-spot, documented AI-context cost). **Do not resurrect the 600-line target** (audited cosmetic, Apr 17).
- [ ] **Phase 6.4: lift NPC voice profile to per-space data flag** — educator-added spaces fall back to narrator voice (accepted); revisit if an educator complains.

### Content / authoring workstreams (each is its own project; pointers only)
- [ ] **Story authoring rollout** — 5 of ~75 card-effect rows authored; **a full draft for most spaces already exists and was never merged: [narratives-draft.md](docs/core/narratives-draft.md)** (Apr 2026; verified 2026-07-18 only the 5 v2.50.0 rows are in SPACE_EFFECTS.csv). Before merging, the draft needs (a) a voice-rule pass — it pre-dates the no-game-language rule and says "Roll" throughout — and (b) format adaptation (it assumed per-type `*_card_narrative` columns; the shipped schema is one `narrative` column per SPACE_EFFECTS row). Two paths: Live Data Editor at `/admin`, or JSON + `scripts/set-narrative.mjs` → `regen-clean-files.mjs`. Priority: high-traffic REG/PM/ARCH/ENG first.
- [ ] **Voice rewrite Pass 2 — ModalConfig.csv population** (~100–150 rows) from the per-space modal tables in [AUTHORED_COPY_REVIEW.md](docs/core/AUTHORED_COPY_REVIEW.md) (Pass 1 — Event/Action/Outcome — verified merged into the CSVs 2026-07-18) — blocked on mapping doc labels → engine `effect_action` keys; worth scripting (parser starting point: `scripts/merge-voice-rewrite.mjs`).
- [ ] **Educational "Learn More" per space** — field-guide register, NYC-sourced (nyc.gov/ZR/Admin Code), 2-space calibration sample first (REG-FDNY-PLAN-EXAM + BANK-FUND-REVIEW).
- [ ] **Per-action confirmation modal two-panel layout** — [Mockups/story-mockup.html](Mockups/story-mockup.html) §2 Option C; POLISH phase.
- [ ] **Editor UX redesign** (Story/Flow/Mechanics tabbed editor, [Mockups/editor-mockup.html](Mockups/editor-mockup.html)) + **Game Card editor** (labeled controls over CARDS_EXPANDED.csv, inline `cardTextMatchesColumns` checks, `_extraColumns` round-trip) — UI pass only, benefits from engine-data separation being further along.
- [ ] **Data storage format (CSV → JSON/markdown)** — decision deferred until engine-data separation reveals the data shape. Do not migrate now.
- [ ] **Multi-tenant catalog (educator licensing/contribution)** — prerequisite is engine-data separation, not file format.
- [ ] **Billing & usage reporting** (per-teacher activity tracking → per-school monthly statements) — build after the teacher layer has real users.

### Balance / metrics (standing watch, not work)
- [ ] **Should a fully dice-determined time cost be exempt from the Try Again penalty? (found 2026-08-15, not a bug — a design question)** — at Investor Review (and Con-Initiation's change-order visit), the space's entire time cost comes from a dice roll with no fixed CSV time-add row, so `tryAgainOnSpace`'s `calculateSpaceTimeAddTotal()` (which only sums fixed rows) charges zero days for negotiating there — confirmed this is what the code actually does, not a preview bug; the negotiate-side cost preview (`Time —`) is accurately reporting a real "negotiating here is free" behavior. Worth a look only if it feels like an exploit in practice: should any space's negotiate penalty include an expected/average value for a dice-based time cost too, not just the fixed portion?
- [ ] **Ghost win-rate "90%" goal** — read win-rate + avgTurns TOGETHER (wins↑ turns→ = easier ✓; wins→ turns↑ = grindier ✗). Track via `.claude/ghost-history.jsonl`. Levers if pursued: easier economy / smarter bot / onboarding.
- [ ] **Game length vs ~40-min class period** — trigger to act: a REAL playtest game runs past ~40 min → trim mechanics/space requirements (that's the lever, not win-rate). Bot avgTurns=149 is NOT a red flag (bots ≠ humans).

### Known limitations / environmental (accepted, flag-only)
- [ ] **Q2 first-visit blind spot** — `scope_changed_since_last_visit` has no snapshot on a first FDNY-FEE-REVIEW visit → defaults "no change"; maintainer accepted 2026-06-15. Sturdier fix if ever needed: snapshot scope at FDNY approval grant.
- [ ] **Loan-repayment deadline + TCO mechanic** (maintainer idea 2026-07-02, "way later") — needs a TCO mechanic; real expansion.
- [ ] **Swap terser → native minifier (Rolldown/Oxc or esbuild)** — bundle-affecting, verify via deploy playtest; preserve vite.config.ts console behaviors. Build is ~8s, low priority.
- [ ] **`CARDS_EXPANDED.csv` carries a stray `\r` mid-row on 386 of 399 rows — INERT, do not "fix" it (investigated 2026-07-29, don't re-chase)** — the file's records end with a bare `\n`, but each one has a carriage return sitting inside column 22 (`work_type_restriction`), which is where the file used to end. **It never reaches the game** — `DataService.parseCsvLine` ([DataService.ts:1009](src/services/DataService.ts:1009)) calls `.trim()` on every field, which strips the `\r` (verified over all 398 data rows: 0 fields retain a `\r`). The one real caveat is that the safety rests on that single `.trim()`, so don't remove it.

### Dependency major-version jumps (deferred 2026-05-29; audit clean, minors done)
- [ ] TypeScript 5.9→6.0 (wait 1–2 months, branch attempt) · jsdom 27→29 (re-test `forksFiles` workarounds) · playwright 1.57→1.60 (alongside Playwright test work).
- [ ] **ESLint 9→10 major-version jump itself is still BLOCKED on `eslint-plugin-react`** (separate from the audit fix above, which no longer depends on this). Confirmed via the npm registry that `eslint-plugin-react` has never released ESLint 10 support — even its latest (7.37.5) declares `peerDependencies: { eslint: "^3 || ... || ^9.7" }`. Forcing eslint to 10.8.0 anyway installs cleanly but crashes every lint run. Re-attempt only after `eslint-plugin-react` ships real 10.x support.
- [ ] **`npm audit` still flags 4 high-severity findings, one advisory chain (`extract-zip`/`@puppeteer/browsers`/`puppeteer`/`puppeteer-core`), deliberately left unpatched 2026-08-14** — `npm audit fix --force` would downgrade `puppeteer` 24.43.1 → 19.8.0 (a large regression) to fix `extract-zip`'s unvalidated-symlink advisory. `puppeteer` is dev-only tooling (screenshot/E2E scripts), never ships to the running server — real risk is a local, one-time archive-extraction step trusting Google's own Chromium CDN. Trigger: re-run `npm audit fix` (no `--force`) next time `puppeteer`/`@puppeteer/browsers` gets bumped for any other reason — check whether a newer minor/patch has picked up a non-vulnerable `extract-zip` without the downgrade. Full reasoning: CHANGELOG v3.2.3.

*For full history, see CHANGELOG.md. Un-promoted v3.0.83–90 feedback staging: [.claude/feedback-staged.md](.claude/feedback-staged.md).*
