/**
 * Re-export from the canonical shared location.
 * This file exists for backward compatibility with dictionary imports.
 */
export { fetchRemoteConfig, type ServiceVisibility } from '../../utils/remoteConfig';
